{
  "name": "selector-to-tag",
  "main": "./lib/main",
  "version": "0.0.1",
  "description": "Create HTML tag elements using CSS selectors in HTML files",
  "repository": "https://github.com/postal2600/selector-to-tag",
  "license": "MIT",
  "engines": {
    "atom": ">0.50.0"
  },
  "dependencies": {
    "atom-helpers": ">= 1.0.0 < 2.0.0"
  }
}
